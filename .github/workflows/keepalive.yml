name: Keep Backend Alive

on:
  schedule:
    - cron: "*/14 * * * *" # 每14分钟运行一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the API
        run: curl -s https://sam-api-backend.onrender.com/ping
